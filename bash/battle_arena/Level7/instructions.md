This level we first figured what needs to be counted which was the files. We then sorted them by size. Finally, we numbered the output.

```
wc -c *.txt
```

next we sort the output by size
```
sort -n
```

next we number the output.
```
nl
```
